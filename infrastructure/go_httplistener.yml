apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: go-httplistener
  namespace: mediapipeline
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      namespace: mediapipeline
      name: gateway-api
      sectionName: rest-listener
  rules:
    - matches:
        - path: 
            type: PathPrefix
            value: /api/v1/file
      backendRefs:
        - kind: Service
          name: go-service
          namespace: mediapipeline
          port: 80