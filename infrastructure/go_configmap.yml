apiVersion: v1
data:
  SERVICE_NAME: "go-service"
  SERVICE_VERSION: "1.0.0"
  APPLICATION_PORT: ":8080"
  ENVIRONMENT: "PROD"
  FILE_COUNTER_GRPC_ADDR: "dotnet-service.mediapipeline.svc.cluster.local"
immutable: true
kind: ConfigMap
metadata:
  name: go-configmap
  namespace: mediapipeline